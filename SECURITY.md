# 安全说明

## 当前部署边界

这是一个本地优先的个人工具，当前没有用户认证、权限控制、CSRF 防护或多租户隔离。不要将开发服务器或生产实例直接暴露到公网，也不要在共享机器上存放未加密的真实投资数据。

请保护好：

- `.env.local` 和数据库路径配置；
- `data/*.db`、WAL 文件和 JSON 备份；
- 任何包含账户、交易或持仓信息的截图和导入文件。

## 报告问题

如果问题可能导致数据泄露、任意文件读取、越权写入或备份被未授权恢复，请不要公开提交完整细节。请通过仓库的私密安全报告渠道联系维护者（正式发布前请在这里补充实际联系地址），并提供复现步骤、影响范围和修复建议。

如果只是一般 bug 或数据口径问题，可以公开创建 issue，但请使用合成数据脱敏复现。
